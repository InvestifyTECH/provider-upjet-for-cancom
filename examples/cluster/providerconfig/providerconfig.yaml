apiVersion: upjet-for-cancom.crossplane.nvst.cloud/v1beta1
kind: ProviderConfig
metadata:
  name: cancom
spec:
  credentials:
    source: Secret
    secretRef:
      name: cancom-creds
      namespace: crossplane-system
      key: credentials
