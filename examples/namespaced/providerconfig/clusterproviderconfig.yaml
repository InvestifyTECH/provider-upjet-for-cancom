apiVersion: upjet-for-cancom.m.crossplane.nvst.cloud/v1beta1
kind: ClusterProviderConfig
metadata:
  name: cancom
spec:
  credentials:
    source: Secret
    secretRef:
      name: cancom-creds
      namespace: crossplane-system
      key: credentials
